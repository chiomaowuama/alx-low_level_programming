#include "main.h"

/**
 * int _isupper - the function to be called.
 * @c: the variable to be used.
 * Return: Always 0.
 */

int _isupper(int x)
{
	if (x >= 65 && x <+ 98)
	{
	return(1);
	}
	return(0);
}
