#include "main.h"
#include <string.h>

/**
 * _strchr - locate character in string
 * @s: the string
 * @c: the character to locate
 *
 * Return: char
*/
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
