#include "main.h"
#include <string.h>
/**
 * _strncpy - Function to be used.
 * @dest: the character.
 * @src: The character.
 * @n: The character.
 * Return: this return th pointer.
 */

char *_strncpy(char *dest, char *src, int n)
{
	return (strncpy(dest, src, n));
}
