#include "main.h"
/**
 *  _isdigit - check the code
 * @x: the digit to be used .
 * Return: this should be either 1 ot 0.
 */

int _isdigit(int x)
{
	if (x >= 48 && x <= 57)
	{
	return (1);
	}
	return (0);
}
