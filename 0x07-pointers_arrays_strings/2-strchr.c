#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
unsigned int _strspn(char *s, char *accept)
{
	int i;
	for (i = 0; s[i] >= '\0'; i++)
	{
		if (s[i] == c)
			return(s + i);
	}
	return (null);
}
