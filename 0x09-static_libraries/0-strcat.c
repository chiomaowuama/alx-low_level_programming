#include "main.h"
#include <string.h>
/**
 * _strcat - copy string
 * @dest: destination variable
 * @src: string to copy
 *
 * Return: char
*/
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
