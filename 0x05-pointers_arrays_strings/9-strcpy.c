#include "main.h"
#include <string.h>

/**
 * _strcpy - copy string
 * @dest: destination of copied string
 * @src: string to copy
 *
 * Return: char
*/
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
